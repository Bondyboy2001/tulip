# Code frames

```python
def fib(n: int) -> int:
    """Docstring."""
    return n if n < 2 else fib(n - 1) + fib(n - 2)
```

```rust
fn main() { println!("{}", 1 + 2); }
```

```
no language here
```
