# Heading

A paragraph with `inline code` and a [[wikilink]].

```js
// a comment
const greet = (name) => `hello ${name}`
export default class Tulip { render () { return 42 } }
```

```python
def fib(n: int) -> int:
    """Docstring."""
    return n if n < 2 else fib(n - 1) + fib(n - 2)
```

```rust
fn main() { println!("{}", 1 + 2); }
```

```
no language here
```

- [ ] a task
- [x] a finished task
