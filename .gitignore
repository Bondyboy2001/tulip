node_modules/
dist/
.DS_Store
*.log

# The packaged .app that scripts/build-app.sh assembles — ~280MB of copied
# Electron, rebuildable from source in one command.
build/

# SwiftPM's cache, left over from the pre-Electron Tulip.
.build/

# Rendered Manim videos in the sample vault — a content-hash cache the
# runner regenerates on demand.
sample-vault/.images/

# Scratch written by agent tooling run inside the repo.
.pi-subagents/
