## What this changes

<!-- What the pull request does, and what it is for. If it fixes an issue,
     "Fixes #12" here will close it on merge. -->

## Why

<!-- The problem, not the patch. A reviewer who disagrees with the fix should
     still be able to agree about what was wrong. -->

## How it was checked

<!-- Tick what you ran. `npm run verify` is all four. -->

- [ ] `npm run lint`
- [ ] `npm run typecheck`
- [ ] `npm test`
- [ ] `npm run build`

<!-- If it touches something with a platform branch, a subprocess, or the
     filesystem, say what you did by hand as well — those are the parts the
     suite cannot reach. -->

## Anything a reviewer should look at first

<!-- Optional. A decision you were unsure about, a trade-off worth arguing
     with, or the file to read before the others. -->
