# Pull request

## What it does


## Why

## How to test
- [ ] `npm run lint` — 0 errors
- [ ] `npm test`
- [ ] `npm run build` (and launch if it touches Electron)

## Checklist
- [ ] No `android`/`mobile`/`capacitor` leftovers outside `package-lock.json`
- [ ] CI still runs on `macos-latest` + `windows-latest`
- [ ] If this touches the vault format, docs in `README.md` updated
